# F1 History — Claude Instructions

## Code style

- TypeScript/Astro files are linted with ESLint and type-checked with `pnpm check` on session stop. Run `pnpm lint --fix && pnpm check` manually if you want to clean up mid-session.
